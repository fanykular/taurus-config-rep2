pipeline {
    agent any
    stages {
    
        stage ('1. Run Load Test using Taurus') {
            steps {
                echo 'Starting test with Taurus'
                echo "duration, ${duration}, users, ${users}."
                sh 'bzt stuff.yml         \
                  
                      -report'
                echo 'Test completed'
            }
        }
    }
}
