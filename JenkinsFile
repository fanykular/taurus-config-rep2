pipeline {
    agent any
    stages {
        stage ('Run Load Test using Taurus') {
            steps {
                echo 'Starting test with Taurus'
                echo "duration, ${duration}, users, ${users}, rampUp, ${rampUp}."
                sh 'bzt stuff.yml         \
                      -o execution.2.ramp-up=%rampUp%      \
                      -o execution.10.concurrency=%users%   \
                      -o execution.100.hold-for=%duration%   \
                      -report'
                echo 'Testddsada completed'
            }
        }
    }
}
